## 编写一个安卓端的自动操作脚本

### 带UI面板支持手动设定脚本触发时间（默认早8:00、晚17:00各一次）

### 具体功能步骤

0.读取屏幕分辨率
1.唤醒屏幕，然后等待1S
2.从屏幕最下方的正中央想让滑动到屏幕中部中心结束，然后等待6S
3.读取屏幕当前页面文字信息，并记录文字坐标备用，这种过程按照实际时间显示弹窗倒计时提示
4.等待2s后点击 刷新签到 的坐标，然后等待1s点击 点击开始签到的坐标，然后等待2S
5.重新读取屏幕当前页面文字信息，并记录文字坐标备用，这种过程按照实际时间显示弹窗倒计时提示
6.然后点击 点击获取定位 的坐标 等待2S后，点击 我已阅读 这几个字的中心坐标
7.然后等待2S后，点击 提交 中心坐标

### 实现方式

追求最快速度以及最稳定的语言，可以js或者python，安装包。目前手机系统版本安卓8.0以上，证书为自签证书。
初始版本号为1.0.0，每次版本有更新都追加1，比如1.0.1、1.0.2

### 目标宿主系统及安卓版本

小米9SE miui14 安卓13

编译出的安装包要在安卓版本大于等于8以上完美运行

### 组件

缺少什么依赖你都自己安装下

### 打包

使用`logo\logo.png`作为apk的应用图标，并且为了在不同屏幕密度的设备上都能清晰显示，你需要以这个`logo\logo.png`为基础准备一套自适应图标，让应用图标在不同设备上可以呈现出圆形、圆角方形、方形等多种形态，以提升系统UI的统一性。

### 自我检查

每次代码编辑完成后都检查有无bug或者语法错误，如果有自己修复。

### 结果

每次代码完成当前阶段任务都在根目录生成最新的daka+版本号.apk
