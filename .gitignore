# 忽略 tools 目录下的压缩包和 JDK 环境
tools/*.zip
tools/jdk-17.0.20.1+1/
tools/jdk/

# 其他非必要大文件 / 可重新下载的内容
tools/android-sdk/
tools/Tesseract4Android/
tools/*.tar.gz
tools/*.aar
tools/chi_sim.traineddata

# 构建产物
build/
*.apk
*.idsig

# 签名密钥(含密码,不入库;由 build.sh 首次构建时自动生成,请另行备份)
keystore.jks

# 本地配置
.claude/settings.local.json

# 桌面 OCR 测试环境（Maven 下载的 jar / 原生 DLL / 临时输出，可随时重建）
tools/tess4j-test/

# 含真实姓名的签到页测试截图，不入库
png/
